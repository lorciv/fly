# Fly

A tool for database migration.

Commands:

* `init`: create metadata structures
* `status`: get list of applied migrations
* `new [name]`: create new migration
* `up`: apply all migrations
* `down`: undo the most recent migration
